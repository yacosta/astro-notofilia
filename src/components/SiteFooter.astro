---
/**
 * Shared site footer for natively-Astro pages (homepage + BlogLayout).
 * Catalog (.dc.html) pages keep the inline equivalent in public/SiteFooter.dc.html.
 */
export interface Props {
  /** Extra tagline under the copyright line. */
  taglineEs?: string;
  taglineEn?: string;
  /** Optional Colección Virtual stats line (homepage). */
  statsLineEs?: string;
  statsLineEn?: string;
  /** Compact centered layout (BlogLayout) vs homepage spacing. */
  variant?: 'home' | 'editorial';
}
const {
  taglineEs = 'Numismática y Notafilia — un catálogo y colección virtual de billetes y monedas históricas.',
  taglineEn,
  statsLineEs,
  statsLineEn,
  variant = 'editorial',
} = Astro.props;

const isHome = variant === 'home';
---
<footer
  class:list={[
    isHome
      ? 'relative z-[1] flex flex-col items-center gap-2 max-w-content mx-auto pt-[72px] px-6 pb-10'
      : 'border-t border-line mt-24 pt-14 pb-10 text-center',
  ]}
>
  <div class:list={[!isHome && 'max-w-content mx-auto px-6 flex flex-col gap-2 items-center']}>
    <span
      data-i18n
      data-en="Copyright 2026 Notofilia. All Rights Reserved."
      class:list={['text-[13px] tracking-[0.18em] uppercase text-muted', isHome && 'font-serif']}
    >Copyright 2026 Notofilia. Todos los derechos reservados.</span>
    {taglineEn ? (
      <span
        data-i18n
        data-en={taglineEn}
        class:list={['italic text-[14px] text-cream-soft', isHome && 'font-serif max-w-full text-center']}
      >{taglineEs}</span>
    ) : (
      <span class:list={['italic text-[14px] text-cream-soft', isHome && 'font-serif max-w-full text-center']}>
        {taglineEs}
      </span>
    )}
    <a
      href="/politica-privacidad-cookies/"
      data-i18n
      data-en="Privacy &amp; Cookie Policy"
      class:list={[
        'text-muted focus-visible:outline focus-visible:outline-2 focus-visible:outline-cream focus-visible:outline-offset-[3px]',
        isHome && 'font-serif text-[13px] underline [text-decoration-color:rgba(183,171,138,0.4)] mt-1',
      ]}
    >Política de Privacidad y Cookies</a>
    {statsLineEs && (
      <p class:list={['font-serif text-[13px] tracking-[0.1em] text-muted text-center m-0 mt-2', isHome && 'max-w-full']}>
        <strong data-i18n data-en="With the entire Virtual Collection:" class="font-bold text-gold-light">Colección Virtual:</strong>
        {' '}
        <span data-i18n set:html={statsLineEs} data-en={statsLineEn}></span>
      </p>
    )}
    <p
      class:list={[
        'm-0 mt-2 text-center text-[13px] tracking-[0.04em] text-muted',
        isHome && 'font-serif',
      ]}
    >
      <span data-i18n data-en="Page designed by">Página diseñada por</span>
      {' '}
      <a
        href="https://cybercon-solutions.com"
        target="_blank"
        rel="noopener noreferrer"
        class="text-cream underline [text-decoration-color:rgba(231,222,201,0.35)] hover:text-gold-light focus-visible:outline focus-visible:outline-2 focus-visible:outline-cream focus-visible:outline-offset-[3px]"
      >CYBERCON SOLUTIONS</a>
      {' '}
      <span data-i18n data-en="with much">con mucho</span>
      {' '}
      <span class="footer-heart" role="img" aria-label="amor">
        <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true" focusable="false">
          <path
            fill="#e07a6a"
            d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"
          ></path>
        </svg>
      </span>
    </p>
  </div>
</footer>
